name: CD

on:
  workflow_call:
    inputs:
      environment:
        description: Environment to deploy to (dev/tst/prd)
        required: true
        type: string
      image:
        description: Docker image to deploy (image_name:image_tag)
        required: true
        type: string
      channel-id:
        description: Slack channel ID for deployment notifications
        required: true
        type: string
      image-set-path:
        description: Comma-separated Helm values path(s) for the container image. See gha-helm deploy.yml for details.
        required: false
        type: string

concurrency:
  group: cd-${{ inputs.environment }}-${{ github.repository }}
  cancel-in-progress: false

jobs:
  tf-plan:
    name: Terraform Plan
    uses: entur/gha-terraform/.github/workflows/plan.yml@v2
    with:
      environment: ${{ inputs.environment }}
    secrets: inherit

  tf-apply:
    name: Terraform Apply
    needs: [tf-plan]
    uses: entur/gha-terraform/.github/workflows/apply.yml@v2
    with:
      environment: ${{ inputs.environment }}
      has_changes: ${{ needs.tf-plan.outputs.has_changes }}
    secrets: inherit

  deploy:
    name: Deploy
    needs: [tf-apply]
    # Terraform apply is skipped on empty plan. This if ensures that job is run even if previous step is skipped
    if: always() && !cancelled() && !contains(needs.*.result, 'failure')
    uses: entur/gha-helm/.github/workflows/deploy.yml@v1
    with:
      environment: ${{ inputs.environment }}
      image: ${{ inputs.image }}
      slack_channel_id: ${{ inputs.channel-id }}
      image_set_path: ${{ inputs.image-set-path }}
    secrets: inherit
